# Kimi handoff state — 2026-09-09 ~15:05 (window 7)

## Governing request
User: "restart all tasks… make sure it can get out of failed and conflict states
and can restart… show the fix loop in the DAG graph… start utilizing worktrees
and PRs and commits… update AGENTS.md and all affected/referenced files."
+ "do all of this and use up to 2 more subagents to break this work up better".
User KILLED the old run-queue (bash-0mqzjv1m): no `main.py code run` alive now.
Clean slate for a controlled requeue with the NEW code (below).

## LANDED this window (uncommitted in working tree, verified)
- Manual merge of cross-page-nav (had passed gate+GLM review at 14:20; original
  merge was blocked by transient dirty static/usage.html): commit 3ccb5c2 on
  main, DB row 'merged', task.merged(repaired=True) event. Its worktree+branch
  removed. model-escalation's empty worktree+branch removed (kimi had produced
  NO file changes across 2 stalled attempts — nothing lost).
- Root cause found: merges failed with "Your local changes …would be
  overwritten" because the blessed repo == operator's working copy. Fixed in
  gitstore.merge_to_main: path-scoped stash push (-u) of blocking dirty files →
  merge → stash pop; pop-conflict keeps merge landed + instructive GitError.
- gitstore.alloc: now ALWAYS `worktree add --force -B task/<tid> wt base_ref`
  (hard-resets branch to base on re-alloc; rejected work can't leak into retry).
- gitstore.branch_ahead(repo, tid, base) helper (rev-list count).
- gitstore.push_and_open_pr: best-effort push branch+main + `gh pr create`;
  returns (url|None, note), NEVER raises. NOTE: repo has NO remote and gh not
  verified → will emit pr_skipped("no git remote configured"). Honest report.
- config.py: MAX_GRAPH_STEPS default 200→1500; ESCALATION_PATH
  (ARC_ESCALATION_PATH, default gpt-oss-120b→DeepSeek-V4-Flash→GLM-5.3→Kimi-K3)
  + MAX_ESCALATIONS (ARC_MAX_ESCALATIONS).
- code_tasks.build_code_graph rewritten:
  * resume: prior rows via store.code_tasks_for(taskfile); 'merged'→make_skip
    stub publish node (dependents treat as done); 'failed' resumes one tier
    higher than row's model (fresh budget); 'conflict' same model; emits
    run.resume{skipped_merged,retried,escalated_on_resume}.
  * escalation: escalate_<tid> node; edges gate/review fail→implement while
    within_budget (mfr*(esc_n+1)), →escalate when can_escalate, else →fail.
    reviewer_for() flips kimi↔glm for strong models. task.escalated event;
    row model/reviewer updated; harness_runs record actual model; fail msg
    "exhausted escalation up to <last>".
  * conflict repair: prior conflict → chain starts at publish_<tid>, which
    first tries branch_ahead+merge+cleanup of the OLD reviewed branch (emits
    task.conflict on failure, distinct from task.failed); edge
    publish→alloc when repair failed (alloc not in results) = full re-run.
  * publish success → _pr_hook (task.pr_opened / task.pr_skipped events).
- store.reset_stale_code_tasks(taskfile=None): optional taskfile scoping.
- main.py cmd_code run path: scoped stale reset + prints resume plan lines.
- VERIFIED: py_compile all six files OK; graph build against real DB for
  escalation-and-nav.json: skip-stub start publish_cross-page-nav + full
  7-node chain for model-escalation, 12 edges, validate() passes; `--dry-run` OK.

## SUBAGENT WORK (both completed, verified by them)
- agent-14 (dashboard viz +82/−18): dashboard.py _task_loop_stats helper
  (attempts/escalations/conflicts/last_verdict per task; used by _projects AND
  _project_detail), fixloop self-edges in dag.edges when attempts>1||esc>0;
  index.html: conflict color #f0883e (orange, distinct from failed red),
  dashed amber self-arcs with xN (+⬆esc) labels in miniDag & renderDag,
  failed-verdict red dot, friendly() cases for task.escalated/task.conflict/
  task.merged/run.resume. py_compile OK, node --check OK, smoke-tested vs real
  db/events. Deviations: attribution falls back to e.module when e.task absent.
- agent-15 (docs): AGENTS.md Rules 3/4/5/7 + workflow; orchestration-contract
  chain diagram + status lifecycle + retry-safety + publish-hook guarantees;
  runbook new "Retrying/resuming" §.5, replaced stale "make reduced taskfile"
  advice. Docs-only, truthful to code behavior above.

## PROGRESS UPDATE (~15:55)
- Dashboard restarted (pid 83514); /api/projects verified showing fixloop
  edges (projects-ui-cleanup x2, graph-patterns-research x2, index-graph-polish
  x3) + real tokens; orange conflict + amber arcs live.
- Fleet queue v2 launched: bash-p883u8dk, pid 83572; order escalation-and-nav
  (-> skip cross-page-nav merged; rerun model-escalation @Kimi) ->
  dashboard-ui-recovery (index-graph-polish; note: its gate expects
  'run.resume' string in main.py -- intentionally left absent? no: main.py has
  the resume code path but the literal 'run.resume' constant only lives in
  code_tasks.py, so gate may engage one fix loop; acceptable live test) ->
  dag-chaining -> github-ops-and-heartbeat -> projects-ui-and-patterns.
- Commits: 61fbbdc (dashboard/config/queue), f9e4d12 (engine+docs; notes
  foreign bench plumbing included). README.md+pool.py foreign-only dirt left.
## STILL TO DO (in order)
1. (done) Restart dashboard (dashboard.py changed): ./stop.sh && ./start.sh; verify
   /api/projects shows attempts/fixloop edges; check no JS errors obvious.
2. Requeue remaining taskfiles with NEW code (recreate run-queue.sh — user
   deleted nothing but killed it): order: escalation-and-nav.json (resume:
   skips cross-page-nav, re-runs model-escalation at Kimi-K3 — its gate greps
   ESCALATION_PATH/task.escalated/resume greps which MY code now satisfies… the
   task may now be a no-op/doc-check; acceptable; or consider DB-marking
   model-escalation 'merged' with note instead = honest since deliverable
   landed by operator), then dag-chaining.json, github-ops-and-heartbeat.json,
   projects-ui-and-patterns.json. Respawn run-queue.sh as background task.
   Watch first minutes for driver.start activity (kiim K3 stall risk known:
   900s DRIVER_TIMEOUT, 2x ~30 min).
3. Commit: contaminated files (code_tasks.py/gitstore.py/main.py/store.py/
   config.py/AGENTS.md/README/orchestration-contract/runbook have foreign bench
   hunks — strategy: commit cleanly-able files first; for contaminated, /tmp
   replay trick or accept a mixed commit with message noting both workstreams.
   dashboard.py + static/* are mine+clean (agent-14 verified only its edits).
4. Update this file + final user report (see below).

## Facts/traps
- Never pkill -f "kimi -p". Dashboard restart: ./stop.sh && ./start.sh.
- kimi+kimi-code share ONE ARC account cap 3 (dashboard flags over_cap).
- kimi stream-json transcripts have NO usage (tokens 0 by design; opencode has).
- Kimi stall pattern: 900s DRIVER_TIMEOUT hits, retries ×4 — escalation-and-nav
  re-run may take ~30+ min for model-escalation at Kimi-K3 cap 2.
- Uncommitted FOREIGN workstreams still in tree (bench.py/bench_data.py/
  orchbench.py untracked; pool.py/README/AGENTS.md bench hunks) — do not revert.
- Usage page user's 6x-asked issues already fixed+committed in bac1187.
- gh CLI: `which` not found on box earlier ("which: command not found" — use
  `command -v gh`); gh auth reportedly done by user; no git remote anywhere →
  PR hook will pr_skip until origin exists. Tell user how to enable:
  create GitHub repo, `git remote add origin …`, push.

## Final report shape (next window)
- restart/recovery semantics (resume: skip merged/escalate failed/repair
  conflict; auto scoped stale-reset; alloc hard-reset; stash-tolerant merges)
- fix loop now visible: Projects DAG amber self-arcs xN, orange=conflict
- PR flow: hook live, but no remote configured → skipped with event; how to enable
- AGENTS.md + contract + runbook updated (subagent)
- fleet restarted: queue contents + what to watch; minecraft clone is next
  after queue (user's big goal; dag-chaining.json implements cross-DAG deps)

## 2026-09-09 16:25 update — Kimi stall root-caused + fixed; fleet v3 running

- **Root cause of Kimi-K3 "stalls"**: NOT permissions (plain `kimi -p` auto-approves all tools headlessly — proven by smoke test; `--auto`/`--yolo` are rejected with `-p`). kimi CLI wire logs (`~/.kimi-code/sessions/wd_model-escalation_*/agents/main/wire.jsonl`) all end on a hanging `llm.request` — ARC holds concurrency-rejected requests open w/o response (docs: per-model caps, "additional requests rejected"; also 8000-token max for NON-streaming requests). Observed: ~70s fast progress (~110KB), then silence until the 900s kill, ×4 attempts = ~60min/task.
- **Fix shipped `6075904`**: `config.DRIVER_IDLE_TIMEOUT=300` (`ARC_DRIVER_IDLE_TIMEOUT`), `drivers._once` kills on 300s of stdout silence; stall cost now ~5min. AGENTS.md Rule 7 updated. Confirmed an `--auto` edit was WRONG and reverted before commit.
- **model-escalation dropped from `~/tasks/escalation-and-nav.json`** (`.bak` kept): deliverable already shipped+manually verified in f9e4d12; re-running risked an agent clobbering it and burned hours on Kimi hangs. DB row stays `failed` (reset-stale) — honest record; dashboard will show it.
- **Queue v3 running: task bash-1f5ngh5f (pid 85315)** `./run-queue.sh`, remaining: dashboard-ui-recovery → dag-chaining → github-ops-and-heartbeat → projects-ui-and-patterns. Live progress confirmed (gpt-oss 55s implement, gate pass, GLM review, parallel GLM run). dry-run of trimmed file passed.
- Uncommitted on purpose: README.md, pool.py (bench-workstream dirt). Never `pkill -f "kimi -p"`.
- NEXT: when queue drains → Minecraft clone live run (user-confirmed next). Prep pattern from ~/tasks/dag-chaining.json (chained DAGs); dry-run first.

## 2026-09-09 17:00 update — leases shipped; stall cascade explained; queue v4 running

- **b48b92b cross-process driver leases**: `store.driver_leases` (WAL sqlite; pid-liveness + 1800s TTL reaping — `DRIVER_LEASE_TTL`). `drivers._lease_acquire` waits over-cap + emits `driver.cap_wait {model, task, in_use, cap}` every 20s poll (≈1/min) = the concurrency warning surface. Unit-verified (cap, dead-pid reap, release). Docs: AGENTS.md Rule 6 (3 layers) + docs/concurrency-limits.md Layer 2b.
- **Stall cascade mechanism confirmed**: ARC rejects at cap with instant 400 (`provider.api_error: 400` seen live) OR holds the request open (the 300s-idle hangs). Every killed/suspended run leaves server-side requests + lease rows + 'running' code_tasks rows that make it WORSE for a while (dashboard inflight.over_cap showed kimi=9/3 — phantom rows from killed runs). SIGSTOP (Ctrl-Z) is the worst case: frozen processes hold everything. `kill -KILL` suspended pairs; never Ctrl-Z the queue.
- **Queue v4: task bash-1pn6aazs (pid 89902)** `./run-queue.sh` background; waits/eliminates nothing — resume resets scoped stale rows per taskfile. Remaining: dag-chaining → github-ops-and-heartbeat → projects-ui-and-patterns (escalation-and-nav merged+done earlier).
- User context: they killed my bg queue earlier and ran it in their terminal believing a kimi instance was "used up" running it — clarify: the queue uses ZERO interactive kimi slots; only short-lived spawned CLIs (lease-capped at 2) + my session. User then Ctrl-Z'd their terminal queue (they said "took a couple tries"); I cleaned + relaunched v4 in background.
- **Minecraft clone live run is NEXT after queue drains** (user-confirmed multiple times). Pattern: chained DAGs per ~/tasks/dag-chaining.json; dry-run first; launch as queue.
- Kimi stall infra summary of today's fixes: DRIVER_IDLE_TIMEOUT=300 kill (6075904), cross-process leases (b48b92b), engine resume/escalation/conflict-repair/PR-hook (f9e4d12).
- Still uncommitted on purpose: README.md, pool.py (bench dirt). Never pkill -f "kimi -p"; kill by pid.
